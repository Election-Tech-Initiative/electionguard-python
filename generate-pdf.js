/* Button code to be placed in html file where button appears.
<div class="col-12 d-grid mb-3">
    <a href="#/admin/generate-pdf" class="btn btn-primary">Generate PDF</a>
  </div>
*/

//Js file code for functionality:
/*import Spinner from "../shared/spinner-component.js";

import { jsPDF } from "jspdf";

const doc = new jsPDF();

export default {
  components: {
    Spinner,
  },
  data() {
    return {
      loading: false,
    };
  },
  methods:
  {
    generatePDF()
    {
      Input as a json from html doc. Format and make printer friendly pdf. Use native printing functionality. Use Steve's video.
      Take a custom css. View Tally Page should be the button location. Take all the html off the page and put it on the pdf and print.
      The section where the code goes is in between the h2 tags or on parent page (move down to column 11 for latter)
      doc.save("ElectionInformation.pdf");
    },
  },
};
*/